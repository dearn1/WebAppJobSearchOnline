@model WebAppJobSearchOnline.Data.JobApplication

@{
    ViewData["Title"] = "Job Applications - Details";
}

<div>
    <h4>Job Applications - Details</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.JobPostingId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.JobPostingId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.UserId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.UserId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Status)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Status)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.AppliedDate)
        </dt>
        <dd class = "col-sm-10">
            @Model.AppliedDate.ToString("dd-MMM-yyyy") <!-- Custom date format -->
        </dd>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.CVFileName))
            {
                <a asp-action="DownloadCV" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-primary">
                    Download @Model.CVFileName
                </a>
            }
            else
            {
                <span>No CV uploaded</span>
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
